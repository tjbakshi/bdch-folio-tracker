# File: .env.local
# COMPLETE REPLACEMENT - Replace your entire .env.local file with this
# Update the placeholder values with your actual credentials

# Required Supabase Environment Variables
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Enhanced monitoring and debugging
SENTRY_DSN_EDGE=your-sentry-dsn-if-you-have-one

# Optional: Vercel deployment (if using Vercel)
VERCEL_TOKEN=your-vercel-token-here

# Optional: Database direct access (usually not needed)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-ref.supabase.co:5432/postgres

# Optional: Additional API keys (if your app uses them)
NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# Security
NEXTAUTH_SECRET=your-nextauth-secret-if-using-auth
NEXTAUTH_URL=http://localhost:3000

# NOTE: For GitHub Actions CI/CD, add these same values as repository secrets:
# GitHub Settings > Secrets and variables > Actions
# - SLACK_WEBHOOK_URL (for notifications)
# - SUPABASE_SERVICE_ROLE_KEY
# - SUPABASE_PROJECT_REF  
# - SUPABASE_URL
# - SUPABASE_ANON_KEY
# - SENTRY_DSN_EDGE (optional)
# - VERCEL_TOKEN (optional)
